{% extends 'base.html.twig' %}

{# {% block title %}Avis{% endblock %} #}

{% block body %}
<div class="container-fluide">
    <header>
        {% include 'nav.html.twig' %}
    </header>
    {% block main %}

    <main id="pAvis">
        <section class="text-center">

            <h1>Avis</h1>

            <table class="table">
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ post.id }}</td>
                    </tr>
                    <tr>
                        <th>Commentaire</th>
                        <td>{{ post.commentaire }}</td>
                    </tr>
                    <tr>
                        <th>Date</th>
                        <td>{{ post.date ? post.date|date('Y-m-d H:i:s') : '' }}</td>
                    </tr>
                    <tr>
                        <th>User</th>
                        <td>{{ post.user }}</td>
                    </tr>
                    {# <tr>
                <th>Reservation</th>
                <td>{{ post.reservation }}</td>
            </tr> #}
                </tbody>
            </table>


            <a href="{{ path('post_index') }}" title="Retour à la liste" class="btn"><i
                    class="fas fa-arrow-circle-left"></i></a>

            <a href="{{ path('post_edit', {'id': post.id}) }}" title="Modifier" class="btn"><i
                    class="fas fa-edit"></i></a>

            {{ include('post/_delete_form.html.twig') }}
        </section>
    </main>
    {% endblock %}
    {% include 'footer.html.twig' %}
</div>
{% endblock %}
{% extends 'base.html.twig' %}

{% block title %}Formule{% endblock %}

{% block body %}
    <h1>Formule</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formule.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ formule.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ formule.description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ formule.prix }}</td>
            </tr>
        </tbody>
    </table>

    

    <a href="{{ path('formule_index') }}">back to list</a>

    <a href="{{ path('formule_edit', {'id': formule.id}) }}">edit</a>

    {{ include('formule/_delete_form.html.twig') }}
{% endblock %}
